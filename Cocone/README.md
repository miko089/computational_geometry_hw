Install
```
cd Cocone
python3 -m venv .venv
.venv/bin/python -m pip install -U pip
.venv/bin/python -m pip install -r requirements.txt
```

Run
```
.venv/bin/python cocone.py
```

Input: `bunny.xyz` (XYZ points). Output: `test.off` (mesh).

