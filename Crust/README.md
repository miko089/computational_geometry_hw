Setup
```bash
cd Crust
python3 -m venv .venv
.venv/bin/python -m pip install -U pip
.venv/bin/python -m pip install -r requirements.txt
```

Run (show plot)
```bash
.venv/bin/python main.py points.txt --plot
```